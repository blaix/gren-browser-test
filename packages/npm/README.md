# Gren Browser Test NPM Package

Initialize a project to run automated browser tests in [Gren](https://gren-lang.org/).

## Usage

```bash
mkdir my-test-dir
cd my-test-dir
npx gren-browser-test init
```

